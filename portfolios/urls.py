"""URL routing for portfolios app.

This module defines URL patterns following the styleguide pattern:
- One URL per API (1:1 mapping)
- Group by domain in domain_patterns lists
- Use include() from main urlpatterns
"""
from django.urls import path

# Domain patterns for portfolios
portfolio_patterns = [
    # URL patterns will be added here in Step 9 when APIs are created
]

urlpatterns = portfolio_patterns

