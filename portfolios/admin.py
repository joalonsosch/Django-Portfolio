"""Django admin configuration for portfolios app.

This module registers models in the Django admin interface.
Models will be registered here as they are created in Step 4.
"""
from django.contrib import admin

# Models will be imported and registered here in Step 4
# Example:
# from .models import Asset, Portfolio
# admin.site.register(Asset)
# admin.site.register(Portfolio)
